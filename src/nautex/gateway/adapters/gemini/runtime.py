"""Gemini CLI — ACP adapter reference.

Launch: gemini --acp
Transport: stdio
Execution model: delegated
Auth: oauth-personal

Per-agent config is in AGENT_REGISTRY (gateway/models.py).
Integration evidence in INTEGRATION_EFFORT_LOG.md.
"""
