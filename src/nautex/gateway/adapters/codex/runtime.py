"""Codex — ACP adapter reference.

Launch: codex-acp
Transport: stdio
Execution model: partial (fs delegated, terminal local)
Auth: chatgpt

Per-agent config is in AGENT_REGISTRY (gateway/models.py).
Integration evidence in INTEGRATION_EFFORT_LOG.md.
"""
